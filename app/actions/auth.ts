'use server';

/**
 * Note: User metadata (user_type, role) is now set during sign-up
 * via the signUp options.data field. No separate function needed.
 * 
 * The user_type is stored in auth.users.user_metadata and can be
 * accessed via user.user_metadata.user_type
 */

